from django.apps import AppConfig


class PrerequisitesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.prerequisites'
    verbose_name = 'Gestion des Prérequis'
